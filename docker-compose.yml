services:
  cc-spend:
    build: .
    image: cc-spend:latest
    pull_policy: never
    restart: unless-stopped
    env_file: /docker/cc-spend/.env
    ports:
      - "9090:9090"
    volumes:
      - /docker/cc-spend:/data
      - /docker/cc-spend/cards.yml:/app/cards.yml:ro
